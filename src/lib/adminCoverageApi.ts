import { adminFetch } from "@/lib/adminApi";
import { coverageParams, validateCoverageResponse } from "@/lib/anmsmCoverage";
import type { CoverageQuery } from "@/types/anmsmCoverage";
export class CoverageApiError extends Error { constructor(public status: number, message: string) { super(message); } }
const message = (status: number) => status === 401 ? "Session administrateur expirée ou absente" : status === 403 ? "Accès administrateur interdit" : status === 404 ? "Endpoint de couverture non déployé" : status === 500 ? "Erreur serveur ; vérifiez que les migrations sont appliquées" : `Endpoint indisponible (HTTP ${status})`;
export async function getAnmsmCoverage(query: CoverageQuery) { let response: Response; try { response = await adminFetch(`/api/admin/anmsm/coverage?${coverageParams(query)}`, { method: "GET", cache: "no-store", headers: { Accept: "application/json" } }); } catch { throw new CoverageApiError(0, "Endpoint indisponible (erreur réseau)"); } if (!response.ok) throw new CoverageApiError(response.status, `${message(response.status)} — HTTP ${response.status}`); let payload: unknown; try { payload = await response.json(); } catch { throw new CoverageApiError(response.status, "Format de réponse invalide : JSON illisible"); } try { return validateCoverageResponse(payload); } catch (error) { throw new CoverageApiError(response.status, error instanceof Error ? error.message : "Format de réponse invalide"); } }
export async function exportAnmsmCoverage(query: CoverageQuery) { const response = await adminFetch(`/api/admin/anmsm/coverage?${coverageParams(query, true)}`, { method: "GET", cache: "no-store", headers: { Accept: "text/csv" } }); if (!response.ok) throw new CoverageApiError(response.status, `Export CSV impossible — HTTP ${response.status}`); const type = response.headers.get("Content-Type") || "text/csv;charset=utf-8"; if (!type.toLowerCase().includes("csv")) throw new CoverageApiError(response.status, `Export CSV invalide (${type})`); const disposition = response.headers.get("Content-Disposition") || ""; const encoded = disposition.match(/filename\*=UTF-8''([^;]+)/i)?.[1]; const plain = disposition.match(/filename="?([^";]+)"?/i)?.[1]; return { blob: await response.blob(), filename: encoded ? decodeURIComponent(encoded) : plain || "stations-a-contacter-anmsm.csv" }; }

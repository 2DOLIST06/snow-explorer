import { useMemo, useState } from "react";
import AccessibleModal from "./AccessibleModal";
import { ChangeList, FilePicker, ImportRules, MessageList, validateJsonFile } from "./ImportUi";
import { confirmBulkStationImport, previewBulkStationImport } from "@/lib/api/stationImports";
import type { BulkImportOptions, BulkImportPreview, BulkImportResult, BulkStationPreview } from "@/types/stationImport";

type Filter = "all" | "update" | "create" | "unchanged" | "error" | "warning";
export default function BulkImportModal({ open, onClose, onImported }: { open: boolean; onClose: () => void; onImported: () => void | Promise<void> }) {
  const [file, setFile] = useState<File | null>(null), [fileError, setFileError] = useState(""); const [options, setOptions] = useState<BulkImportOptions>({ create_missing: false, transaction: "atomic" }); const [preview, setPreview] = useState<BulkImportPreview | null>(null), [result, setResult] = useState<BulkImportResult | null>(null); const [detail, setDetail] = useState<BulkStationPreview | null>(null), [filter, setFilter] = useState<Filter>("all"), [busy, setBusy] = useState(false), [error, setError] = useState("");
  const reset = () => { setFile(null); setFileError(""); setOptions({ create_missing: false, transaction: "atomic" }); setPreview(null); setResult(null); setDetail(null); setFilter("all"); setError(""); };
  const close = () => { if (!busy) { reset(); onClose(); } }; const select = (f: File | null) => { setFile(f); setFileError(f ? validateJsonFile(f) || "" : ""); setPreview(null); setResult(null); };
  const analyze = async () => { if (!file) return; setBusy(true); setError(""); try { setPreview(await previewBulkStationImport(file, options)); } catch (e) { setError(e instanceof Error ? e.message : "Analyse impossible."); } finally { setBusy(false); } };
  const blocked = !preview?.valid || !preview.preview_token || preview.summary.changed_fields === 0 || preview.errors.some(e => e.blocking !== false) || (preview.summary.creations > 0 && !options.create_missing);
  const confirm = async () => { if (!file || !preview?.preview_token || blocked) return; setBusy(true); try { setResult(await confirmBulkStationImport(file, preview.preview_token, options)); await onImported(); } catch (e) { setError(e instanceof Error ? e.message : "Import impossible."); } finally { setBusy(false); } };
  const rows = useMemo(() => (preview?.stations || []).filter(s => filter === "all" || (filter === "warning" ? s.warnings.length > 0 : s.status === filter)), [preview, filter]);
  return <AccessibleModal open={open} title="Importer plusieurs stations" busy={busy} onClose={close}><div className="import-modal__body" aria-live="polite">{error && <div className="import-alert import-alert--error" role="alert">{error}</div>}{!result && <><FilePicker file={file} onChange={select} error={fileError} /><fieldset className="import-options"><legend>Options</legend><label><input type="checkbox" checked={options.create_missing} onChange={e => { setOptions(o => ({ ...o, create_missing: e.target.checked })); setPreview(null); }} /> Créer les stations absentes</label>{options.create_missing && <p className="import-alert import-alert--warning">Les stations absentes du site pourront être créées lors de la confirmation.</p>}<label><input type="radio" name="transaction" checked={options.transaction === "atomic"} onChange={() => setOptions(o => ({ ...o, transaction: "atomic" }))} /> Annuler tout l’import si une erreur survient</label><label><input type="radio" name="transaction" checked={options.transaction === "valid_only"} onChange={() => setOptions(o => ({ ...o, transaction: "valid_only" }))} /> Importer uniquement les stations valides</label></fieldset>{!preview && <><ImportRules /><button className="btn btn--primary" disabled={!file || !!fileError || busy} onClick={analyze}>{busy ? "Analyse…" : "Analyser le fichier"}</button></>}</>}{preview && !result && <><div className="import-summary">{Object.entries(preview.summary).map(([key, value]) => <div key={key}><span>{({ total: "Total détecté", existing: "Existantes", updates: "À mettre à jour", missing: "Absentes", creations: "À créer", unchanged: "Inchangées", errors: "En erreur", changed_fields: "Champs modifiés" } as Record<string, string>)[key]}</span><strong>{value}</strong></div>)}</div><MessageList title="Erreurs" messages={preview.errors} /><MessageList title="Avertissements" messages={preview.warnings} warning /><label className="import-filter">Filtrer <select value={filter} onChange={e => setFilter(e.target.value as Filter)}><option value="all">Toutes</option><option value="update">Mises à jour</option><option value="create">Créations</option><option value="unchanged">Inchangées</option><option value="error">Erreurs</option><option value="warning">Avertissements</option></select></label><div className="import-table-wrap"><table className="import-table"><thead><tr><th scope="col">Nom</th><th scope="col">Slug</th><th scope="col">Identifiant</th><th scope="col">Statut</th><th scope="col">Changements</th><th scope="col">Erreurs</th><th scope="col">Action</th></tr></thead><tbody>{rows.map(s => <tr key={`${s.target.id}-${s.target.slug}`}><td>{s.target.name}</td><td>{s.target.slug}</td><td>{s.target.id}</td><td>{s.status}</td><td>{s.changes.length}</td><td>{s.errors.length}</td><td><button className="import-link" onClick={() => setDetail(s)}>Voir le détail</button></td></tr>)}</tbody></table></div>{detail && <section className="import-detail"><button className="import-link" onClick={() => setDetail(null)}>Fermer le détail</button><h3>{detail.target.name} — {detail.status}</h3><p>Ancienne identité : {detail.previous_identity?.name || "—"} ({detail.previous_identity?.slug || "—"})</p><p>Nouvelle identité : {detail.new_identity?.name || detail.target.name} ({detail.new_identity?.slug || detail.target.slug})</p><MessageList title="Erreurs" messages={detail.errors} /><MessageList title="Avertissements" messages={detail.warnings} warning /><ChangeList changes={detail.changes} /></section>}<div className="import-actions"><button className="btn btn--secondary" disabled={busy} onClick={() => setPreview(null)}>Modifier les options</button><button className="btn btn--primary" disabled={blocked || busy} onClick={confirm}>{busy ? "Confirmation…" : "Confirmer l’import"}</button></div></>}{result && <div className="import-result"><h3>Rapport d’import</h3><p>{result.message || (result.success ? "Import terminé." : "Import partiel ou en erreur.")}</p><div className="import-summary">{Object.entries(result.summary).map(([k, v]) => <div key={k}><span>{k}</span><strong>{v}</strong></div>)}</div><button className="btn btn--primary" onClick={close}>Fermer</button></div>}</div></AccessibleModal>;
}
